(function(){"use strict";const button=document.querySelector("[data-share-card]");const status=document.querySelector("[data-share-status]");if(!button)return;button.addEventListener("click",async function(){const data={title:"QIZE Digital Business Card",text:"QIZE — Entry-Door Hardware Systems",url:window.location.href};try{if(navigator.share)await navigator.share(data);else{await navigator.clipboard.writeText(window.location.href);status.textContent="Card link copied."}}catch(error){if(error&&error.name!=="AbortError")status.textContent="Copy the page address to share this card."}})})();
